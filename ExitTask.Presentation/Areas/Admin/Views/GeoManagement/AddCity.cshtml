@using Resources
@model ExitTask.Presentation.Areas.Admin.Models.CityViewModel

@{
    this.ViewBag.Title = "AddCity";
    this.Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>AddCity</h2>

@using (this.Html.BeginForm("AddCity", "GeoManagement", FormMethod.Post))
{
    @(this.Html.AntiForgeryToken())

    <div class="form-horizontal">
        <h4>CityViewModel</h4>
        <hr/>
        <fieldset>
            <legend>Legend</legend>
            @(this.Html.ValidationSummary(true, "", new { @class = "text-danger" }))
            <div class="form-group">
                <div class="col-md-10">
                    @(this.Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "form-control floating-label", placeholder = Resource.Name } }))
                    @(this.Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" }))
                </div>
            </div>

            <div class="form-group">
                <div class="col-md-10">
                    @(this.Html.TextAreaFor(model => model.Description, new { @class = "form-control floating-label", placeholder = Resource.Description, rows = "5" } ))
                    @(this.Html.ValidationMessageFor(model => model.Description, "", new { @class = "text-danger" }))
                </div>
            </div>

            <div class="form-group">
                <div class="col-md-10">
                    @(this.Html.DropDownListFor(model => model.CountryId, this.Model.Countries, new { @class = "form-control floating-label", placeholder = Resource.Country }))
                    @(this.Html.ValidationMessageFor(model => model.CountryId, "", new { @class = "text-danger" }))
                </div>
            </div>

            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Create" class="btn btn-default"/>
                </div>
            </div>
        </fieldset>
    </div>
}